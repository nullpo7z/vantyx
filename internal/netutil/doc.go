// Package netutil contains small reusable networking helpers, currently
// focused on safe formatting of IPv6 endpoints (joining hosts with
// ports while inserting the bracket form that net.JoinHostPort needs).
package netutil
